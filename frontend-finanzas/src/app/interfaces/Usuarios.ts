export interface Usuarios {
    id: number;
    nombre: string; 
    email: string;
    password: string;
}

export interface Nuevousuario {
  
    nombre: string; 
    email: string;
    password: string;
}

export interface loginUsuario {
    nombre: string; 
    password: string;
}